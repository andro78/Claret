namespace Claret.Services
{
    /// <summary>Which modem protocol a serial file send uses.</summary>
    public enum FileTransferProtocol
    {
        Xmodem,
        Ymodem,
        Zmodem,
    }
}
