<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="XCENA_Terminal_Dev.Controls.HighlightRulesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XCENA_Terminal_Dev.Controls"
    xmlns:models="using:XCENA_Terminal_Dev.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <local:HexBrushConverter x:Key="HexBrush" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="4,2,2,6" ColumnSpacing="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="Highlight in terminal" />

            <Button x:Name="EditButton"
                    Grid.Column="1"
                    Width="28"
                    Height="26"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    IsEnabled="False"
                    ToolTipService.ToolTip="Edit"
                    Click="OnEditClick">
                <FontIcon Glyph="&#xE70F;" FontSize="13" />
            </Button>

            <Button x:Name="DeleteButton"
                    Grid.Column="2"
                    Width="28"
                    Height="26"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    IsEnabled="False"
                    ToolTipService.ToolTip="Delete"
                    Click="OnDeleteClick">
                <FontIcon Glyph="&#xE74D;" FontSize="13" />
            </Button>
        </Grid>

        <ListView x:Name="RuleList"
                  Grid.Row="1"
                  SelectionMode="Single"
                  SelectionChanged="OnSelectionChanged"
                  DoubleTapped="OnListDoubleTapped">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:HighlightRule">
                    <Grid ColumnSpacing="8" Padding="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- The check box is the rule on/off switch; Click fires only for the user. -->
                        <CheckBox Grid.Column="0"
                                  MinWidth="0"
                                  Margin="0,0,-8,0"
                                  IsChecked="{x:Bind Enabled, Mode=TwoWay}"
                                  Click="OnEnabledClick" />

                        <!-- Filled for a block rule, outlined for a text-only one. -->
                        <Border Grid.Column="1"
                                Width="14"
                                Height="14"
                                CornerRadius="3"
                                BorderThickness="1"
                                VerticalAlignment="Center"
                                BorderBrush="{x:Bind EffectiveColor, Mode=OneWay, Converter={StaticResource HexBrush}}"
                                Background="{x:Bind SwatchFill, Mode=OneWay, Converter={StaticResource HexBrush}}" />

                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Pattern}"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind Summary}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="EmptyState"
                   Grid.Row="1"
                   Margin="12"
                   TextAlignment="Center"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Text="No rules yet. Add one and any matching text is coloured wherever it appears in the terminal." />

        <Button x:Name="AddButton"
                Grid.Row="2"
                Margin="4,6,2,2"
                HorizontalAlignment="Stretch"
                Style="{StaticResource AccentButtonStyle}"
                Click="OnAddClick">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE710;" FontSize="13" />
                <TextBlock Text="Add rule" />
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
