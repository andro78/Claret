<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="XCENA_Terminal_Dev.Controls.TriggersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:XCENA_Terminal_Dev.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="4,2,2,6" ColumnSpacing="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="Triggers" />

            <Button x:Name="EditButton"
                    Grid.Column="1"
                    Width="28"
                    Height="26"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    IsEnabled="False"
                    ToolTipService.ToolTip="Edit"
                    Click="OnEditClick">
                <FontIcon Glyph="&#xE70F;" FontSize="13" />
            </Button>

            <Button x:Name="DeleteButton"
                    Grid.Column="2"
                    Width="28"
                    Height="26"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    IsEnabled="False"
                    ToolTipService.ToolTip="Delete"
                    Click="OnDeleteClick">
                <FontIcon Glyph="&#xE74D;" FontSize="13" />
            </Button>
        </Grid>

        <ListView x:Name="TriggerList"
                  Grid.Row="1"
                  SelectionMode="Single"
                  SelectionChanged="OnSelectionChanged"
                  DoubleTapped="OnListDoubleTapped">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:TriggerRule">
                    <Grid ColumnSpacing="8" Padding="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- The check box arms and disarms the trigger; Click fires only for the user. -->
                        <CheckBox Grid.Column="0"
                                  MinWidth="0"
                                  Margin="0,0,-8,0"
                                  IsChecked="{x:Bind Enabled, Mode=TwoWay}"
                                  Click="OnEnabledClick" />

                        <FontIcon Grid.Column="1"
                                  FontSize="14"
                                  VerticalAlignment="Center"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  Glyph="{x:Bind Glyph}" />

                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Pattern}"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind Summary}"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="EmptyState"
                   Grid.Row="1"
                   Margin="12"
                   TextAlignment="Center"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Text="No triggers yet. Add one to watch every session for a line and act on it — alert you, start a recording, or answer it." />

        <Button x:Name="AddButton"
                Grid.Row="2"
                Margin="4,6,2,2"
                HorizontalAlignment="Stretch"
                Click="OnAddClick">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE710;" FontSize="13" />
                <TextBlock Text="Add trigger" />
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
